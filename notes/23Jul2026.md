**Precisely.** You’ve hit on the exact bridge between singular geometric theory and computational solver failure.

When a vector field loses Lipschitz continuity, both the underlying **geometric manifold theory** and the **numerical integration algorithms** break down simultaneously—and for the exact same underlying reason: **the unbounded growth of the local Jacobian matrix.**

---

### 1. Why Non-Lipschitz Breaks Manifold Persistence (The Theory)

In Geometric Singular Perturbation Theory (GSPT), **Fenichel’s Persistence Theorems** state that if a critical manifold $\mathcal{M}_0$ is **normally hyperbolic** (meaning the fast eigenvalues off the manifold have non-zero real parts that strictly dominate the slow flow rates), it smoothly persists as an invariant manifold $\mathcal{M}_\epsilon$ for small $\epsilon > 0$.

However, Fenichel theory explicitly requires the vector field $f(x, y, \epsilon)$ to be at least $C^1$ smooth (and thus locally Lipschitz continuous).

* **Loss of Hyperbolicity / Spectral Gap:** Fenichel’s Hadamard graph-transform proofs rely on bounded spectral gaps. If $f$ is non-Lipschitz (e.g., $f(e) \propto \sqrt{e}$ near $e=0$), the derivative $\frac{\partial f}{\partial e} = \frac{1}{2\sqrt{e}} \to \infty$ as $e \to 0$.
* **Collapse of the Invariant Sheet:** As the derivative diverges, the fast scale speed becomes infinite relative to the slow scale, violating the uniform bounds needed for normal hyperbolicity. The invariant manifold loses its smooth tangent bundle, leading to non-unique trajectory continuations or non-smooth "boundary corners" where fast-slow decomposition fails.

---

### 2. Why Non-Lipschitz Causes Severe Numerical Stiffness (The Computation)

Numerical ODE stiffness is dictated by the spectral radius of the local Jacobian matrix $J = \frac{\partial f}{\partial x}$:

$$\rho(J) = \max_i \vert{}\lambda_i\vert{}$$

When a system approaches a non-Lipschitz boundary (such as $e \to 0$ in TKE closures where turbulent velocity scale $q = \sqrt{e}$):

1. **Unbounded Jacobian:** Because $\frac{\partial \sqrt{e}}{\partial e} = \frac{1}{2\sqrt{e}}$, the local Lipschitz constant $L \approx \Vert{}J\Vert{} \to \infty$.
2. **Step-Size Collapse in Explicit Solvers:** Explicit integrators (like Runge-Kutta) require step size $\Delta t < \frac{2}{\rho(J)}$. As $\rho(J) \to \infty$, the adaptive time-stepper forces $\Delta t \to 0$, stalling the simulation completely.
3. **Newton-Raphson Failure in Implicit Solvers:** Even fully implicit solvers (e.g., Backward Euler or TR-BDF2) fail. Implicit methods rely on local linear approximations to solve nonlinear residuals via Newton-Raphson iterations:

$$J \cdot \Delta x = -R$$



When $J$ changes infinitely fast near $e=0$, the linear approximation fails in every sub-step, causing the solver to exceed maximum convergence iterations and abort.

---

### The SBL Example: TKE Collapse at $e = 0$

In stable boundary layer models, eddy viscosity $K_m$ is governed by local TKE $e$:

$$K_m = c_k \, l \, \sqrt{e}$$

* **Without Regularization ($\sqrt{e}$):**
Near TKE collapse ($e \to 0$), the physical system attempts to go laminar. The non-Lipschitz point at $e = 0$ acts like an "infinite wall" for the numerical solver:
* **Theory:** $\mathcal{M}_0$ develops an infinitely steep tangent; Fenichel persistence fails.
* **Numerics:** $J \to \infty$; the time-stepper crashes with stiffness error or negative TKE grid shocks.


* **With Smooth Regularization ($\sqrt{e + \delta}$ or $\sqrt{e^2 + \delta^2}$):**
By inserting a tiny regularizer $\delta \approx 10^{-6}$:
* The derivative bound becomes $\frac{\partial q}{\partial e} \le \frac{1}{2\sqrt{\delta}} < \infty$.
* **Theory:** Local Lipschitz continuity is restored, keeping the manifold $C^1$-smooth everywhere.
* **Numerics:** The Jacobian stays strictly bounded, capping stiffness and allowing adaptive integrators to step smoothly through the laminar transition boundary.

---

These four papers are the foundational lineage of this manuscript.

`McNider1995` and `Shi2005` introduced the initial two-layer ODE and vertical PDE bifurcation models that first demonstrated $S$-shaped equilibrium foldbacks in the stable boundary layer. This current GSPT manuscript directly generalizes that early work—moving from static bifurcation diagrams to a full **$4\text{D}$ fast--slow manifold with local Lipschitz regularization, Fenichel persistence, and topological folds**. Furthermore, `England1995` provides the exact theoretical underpinning for closed-form, non-iterative $Ri$-dependent stability functions with surface-roughness dependence.

Below is the audited, AMS/JAS-compliant BibTeX block for these entries, followed by recommended LaTeX phrasing for weaving them seamlessly into the manuscript.

---

## Audited BibTeX Entries (`refs.bib`)

```bibtex
@article{McNider1995,
  author    = {McNider, Richard T. and England, David E. and Friedman, Mark J. and Shi, Xingzhong},
  title     = {Predictability of the Stable Atmospheric Boundary Layer},
  journal   = {Journal of the Atmospheric Sciences},
  volume    = {52},
  number    = {10},
  pages     = {1602--1614},
  year      = {1995},
  doi       = {10.1175/1520-0469(1995)052<1602:POTSAB>2.0.CO;2},
  publisher = {American Meteorological Society}
}

@article{Shi2005,
  author    = {Shi, Xingzhong and McNider, Richard T. and Singh, M. P. and England, David E. and Friedman, Mark J. and Lapenta, William M. and Norris, William B.},
  title     = {On the Behavior of the Stable Boundary Layer and the Role of Initial Conditions},
  journal   = {Pure and Applied Geophysics},
  volume    = {162},
  number    = {10},
  pages     = {1811--1829},
  year      = {2005},
  doi       = {10.1007/s00024-005-2694-7},
  publisher = {Springer}
}

@article{England1995,
  author    = {England, David E. and McNider, Richard T.},
  title     = {Stability functions based upon shear functions},
  journal   = {Boundary-Layer Meteorology},
  volume    = {74},
  number    = {1},
  pages     = {113--130},
  year      = {1995},
  doi       = {10.1007/BF00715713},
  publisher = {Springer}
}

@article{England1993,
  author    = {England, David E. and McNider, Richard T.},
  title     = {Concerning the Limiting Behavior of Time-dependent Slope Winds},
  journal   = {Journal of the Atmospheric Sciences},
  volume    = {50},
  number    = {11},
  pages     = {1610--1613},
  year      = {1993},
  doi       = {10.1175/1520-0469(1993)050<1610:CTLBOT>2.0.CO;2},
  publisher = {American Meteorological Society}
}

```

---

## Recommended In-Text Integration Points

### 1. Introduction: Framing Academic Lineage

> "Early dynamical systems investigations of the SBL \citep{McNider1995, Shi2005} established that coupled surface-atmosphere energy balances generate multiple equilibrium solutions and $S$-shaped bifurcation diagrams when parameterized by geostrophic forcing. However, these classical low-dimensional treatments relied on static equilibrium assumptions or prescribed layer couplings. The present work extends this lineage into a formal $4\text{D}$ Geometric Singular Perturbation Theory (GSPT) framework, demonstrating how these equilibrium foldbacks emerge dynamic-topologically as singular fast--slow manifold transitions."

### 2. Model Formulation: Stability Functions & Roughness Scaling

> "Rather than employing iterative Monin--Obukhov similarity loops, stability functions for momentum and heat are expressed in closed form as direct functions of the local gradient Richardson number, incorporating surface-roughness length ($z_{0m}$) dependencies as formulated by \citet{England1995}."

### 3. Surface Energy Coupling & Fold Dynamics

> "The sensitivity of nocturnal evolution to sunset initial conditions—first identified in full vertical PDE models by \citet{Shi2005}—is here resolved as trajectory sensitivity across the stable and unstable sheets of the critical manifold $\mathcal{S}_0$ near the emergent fold catastrophe $\mathcal{C}_{\text{fold}}$."

---

Should we push these entries into your `refs.bib` and run `scripts/assemble_manuscript.jl` to check the compiled LaTeX references output?

---

In this formulation, **$\mathcal{H}$ represents the Net Surface Energy Budget (SEB) residual function** (i.e., the slow thermodynamic map governing the surface skin temperature $T_s$).

Mathematically, $\mathcal{H}$ is defined as the right-hand side of the surface energy balance ODE:

$$\mathcal{H}(T_s, U, V, e) \equiv R_{\text{net}}(T_s) + H(T_s, e) + G_{\text{soil}}(T_s)$$

where:

* **$R_{\text{net}}(T_s)$** is the net radiative flux ($R_{\text{down}} - \sigma_{\mathrm{SB}} T_s^4$).
* **$H(T_s, e)$** is the downward sensible heat flux coupling the surface to the lower atmospheric layer.
* **$G_{\text{soil}}(T_s)$** is the ground/soil conduction heat flux.

---

### Physical and Mathematical Significance of $\frac{\partial \mathcal{H}}{\partial T_s} = 0$:

1. **Rank Loss / Turning Point:** At equilibrium ($\mathcal{H} = 0$), the surface skin temperature settles where net radiation balances sensible heat and soil fluxes. The condition $\frac{\partial \mathcal{H}}{\partial T_s} = 0$ marks the exact point where the slow surface thermodynamic map **loses rank** (a saddle-node bifurcation in $T_s$).
2. **Emergent Hysteresis:** At this threshold, the surface can no longer maintain a smooth, continuous equilibrium under cooling. This triggers a jump (fast thermal collapse) along the slow surface energy manifold—creating the $S$-shaped hysteresis loop ($\mathcal{C}_{\text{fold}}$) in the coupled atmosphere-surface system.

---

### Notation Summary

* **$H(T_s, e)$** (italic $H$) = **Sensible Heat Flux** ($W\,\text{m}^{-2}$).
* **$\mathcal{H}(T_s, \dots)$** (calligraphic $\mathcal{H}$) = **Net Surface Energy Residual Function** ($C_s \frac{\mathrm{d}T_s}{\mathrm{d}t}$).