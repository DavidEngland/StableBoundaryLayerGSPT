\subsection{Single-Column Model Verification and Observation Comparison}
\label{subsec:numerical_verification_compat}

This template is retained as a compatibility shim for older manuscript builds. The active comparison section is now defined in \texttt{comparative\_metrics.tex.mustache} so the site-comparison labels and table identifiers live in one place only.

If this file is ever re-enabled in the assembly pipeline, it should continue to avoid redefining \verb|subsec:numerical_verification| and \verb|tab:site_comparison_metrics|.